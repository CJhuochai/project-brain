# Project Brain 1.0 本地验收记录

验收日期：2026-08-24

## 环境与安全边界

- 工作区：`E:\BasisProject`
- 发现仓库：30；完成基线索引：29；索引文件：11,172；待更新文件：0。
- 只读取本地 Git 的 `origin/HEAD` 与对象库；验收前后业务工作区均未被工具修改。

## 验收一：招生事务优化需求

输入为本机需求文档《BG系统_E-2.8.8_招生事务优化需求文档_2026.7.31》，包含访校活动发布范围、报名截止、批量学生查询及短链接等描述。分析首选仓库为 `basis-school-student`，并定位到 `VisitActivityController` 的新增、编辑、发布、取消、列表和 H5 列表方法；同时将 `database`、设施服务及小程序服务标为关联候选。结果明确提示跨仓库接口与发布顺序风险。

## 验收二：融资工具需求

输入为本机需求文档《F-2.1.4.8 B 融资工具、贷款&银行帐户优化》，包含贷款批复、放款、担保条件、监管要求和到期提醒。分析首选仓库为 `basis-finance`（38 条证据），入口定位到 `ApprovalController` 的列表、导出及新增相关方法；调用点定位到 `ApprovalApplication` 对审批、担保条件、监管要求和放款应用的调用。调用链中识别出 `t_finance_loan_approval` 等数据表关系。

## 验收三：本地 Git 二次开发影响

以本地提交 `9bb9c7f287` 为输入，工具精确定位到 `basis-school-student` 的两处变更：订单设施适配器与学生订单应用服务。输出同时包含该两文件的索引证据；未把其他仓库的 HEAD 误认为同一提交。

## 结论与已知边界

1. MCP 可自动增量刷新默认基线，并在需求或提交输入后返回候选仓库、入口、修改/关联点、调用链、风险和待确认项。
2. 中文长需求采用三字业务片段，避免“招生”“学生”等泛词主导结果；影响追踪最多处理 8 个方法入口、每个入口最多返回 20 条路径，避免长需求阻塞。
3. 结果是基于静态证据的候选与确定关系；反射、动态 SQL、运行时路由及未命中的原型字段仍需在开发前人工确认。
