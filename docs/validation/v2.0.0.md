# v2.0.0 验证记录

## 自动化检查

- `go test ./... -count=1`：通过全部产品包；真实工作区 opt-in 基准未由该命令执行。
- `go vet ./...`：通过。
- 回归覆盖：精确与规范化检索、中文/驼峰、FTS 错误、排序前候选截断、重复关键词预算、仓库范围校验；稳定符号与同参数数量重载歧义；深度/路径/循环/诊断缺口；HTTP 服务与 verb 匹配、错误 URL 与注解字段、Dubbo group/version 与本地接口声明、MQ 类型与 broker；摘要/详情和展开；旧 schema 自动重建、旧快照不变、报告保留、迁移失败保留活跃快照。
- 四平台构建与发布布局检查：通过 Windows x64、Linux x64、macOS x64、macOS arm64；跨平台构建不等于目标系统实际运行验证。
- 本地插件源与安装缓存：0.2.0，正式 `codex plugin add` 安装成功；新会话技能行为尚未实测。

## 真实工作区

最终代码的 `scripts/verify-v2.ps1` 验收正在运行，完成后补充实际结果。脚本仅读取仓库，比较前后的 Git 状态与 refs，不执行 fetch、checkout、业务构建或业务测试。首次全量索引耗时另行记录，不声称已有性能提升。

## 未覆盖

动态服务发现、反射、运行时代理、复杂 YAML 配置、其他未测试的框架声明形式和生产部署可达性。框架不明确或静态契约有歧义时保留诊断与未解析状态，不补猜测连线。
